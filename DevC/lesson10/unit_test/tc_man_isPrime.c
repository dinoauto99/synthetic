#include <stdio.h>
#include <assert.h>

int main()
{
    int prime_nums[] = {2,3,5,7,11,13};
    printf("Running manual test:\n");
    for (int i = 0; i < (sizeof(prime_nums) / sizeof(int)); i++)
    {
        assert()
    }
    
}